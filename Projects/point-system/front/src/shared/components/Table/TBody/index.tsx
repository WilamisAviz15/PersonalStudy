import TBodyRow from "./TBodyRow";

const TBody = ({ tBodyData }: { tBodyData: any[] }) => {
  return (
    <tbody>
      {/* {tBodyData.map((item) => {
        return <TBodyRow key={item.id} data={item} />;
      })} */}
    </tbody>
  );
};

export default TBody;
